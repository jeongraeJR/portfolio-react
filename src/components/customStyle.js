import styled from 'styled-components';

const TitleContainer = styled.div`
    margin-top: 60px;
`;

const SubTitleContainer = styled.div`
    margin-top: 30px;
`;

const ContentContainer = styled.div`
    margin-top: 30px;
    font-size: 16pt;
`;

export { TitleContainer, ContentContainer, SubTitleContainer };
